# Optional — leave UNSET on Vercel (recommended).
# Vercel proxies /api → Render automatically via vercel.json.
# Only set this for local overrides, e.g. pointing at production Render directly:
# VITE_API_URL=https://rialu-graph.onrender.com/api
