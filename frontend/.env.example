# Copy to frontend/.env.local for local development (never commit)

# Leave unset locally — Vite proxy handles /api in dev.
# On Vercel, set to your Render backend URL including /api suffix:
# VITE_API_URL=https://rialu-graph-api.onrender.com/api
